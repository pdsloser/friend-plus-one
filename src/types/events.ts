export type PublicationStatus = "draft" | "pending_review" | "revision_required" | "published" | "unlisted";
export type LifecycleStatus = "scheduled" | "completed" | "cancelled";
export type SeatType = "host" | "direct_invite" | "vip_invite" | "public_application" | "plus_one" | "reserved";
export type SeatStatus = "open" | "reserved" | "invitation_pending" | "acceptance_pending" | "confirmed" | "released" | "cancelled";
export type ParticipantVisibility = "profile" | "background_only" | "hidden";
export type EventSeat = { seatNumber: number; seatType: SeatType; seatStatus: SeatStatus; displayLabel: string; participant?: { visibility: ParticipantVisibility; displayName?: string; backgroundTitle?: string; bio?: string; avatarUrl?: string } };
export type EventRecord = { id: string; slug: string; title: string; category: { name: string; slug: string }; tags: string[]; summary: string; description: string; discussionDirection: string; topicPrompts: string[]; suitableFor: string; feeNote: string; minimumSpendNote?: string; publicNotes?: string; coverImageUrl: string; startsAt: string; endsAt: string; timezone: "Asia/Taipei"; city: string; district: string; applicationOpensAt?: string; applicationDeadline?: string; waitlistEnabled: boolean; publicationStatus: PublicationStatus; lifecycleStatus: LifecycleStatus; host: { displayName: string; backgroundTitle: string; bio: string; avatarUrl?: string }; seats: EventSeat[]; privateVenue: { name: string; address: string; meetingInstructions: string; contact: string } };
export type PublicEventSummary = { slug: string; title: string; categoryName: string; categorySlug: string; tags: string[]; summary: string; coverImageUrl: string; startsAt: string; endsAt: string; city: string; district: string; applicationDeadline?: string; waitlistEnabled: boolean; seatCount: number; confirmedSeatCount: number; host: EventRecord["host"] };
export type PublicEventDetail = PublicEventSummary & { description: string; discussionDirection: string; topicPrompts: string[]; suitableFor: string; feeNote: string; minimumSpendNote?: string; publicNotes?: string; seats: EventSeat[] };
